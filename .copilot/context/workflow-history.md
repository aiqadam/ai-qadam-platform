# Workflow History

This file records lessons learned, patterns discovered, and recurring issues found across completed workflows.

---

## Recurring Patterns

*(populated as workflows complete)*

---

## Retrospectives

*(populated after each completed or failed workflow)*

---

## Workflow close-out log (migrated 2026-07-26 from workspace-state.md)

> These entries were previously prepended to `.copilot/context/workspace-state.md`,
> which is now a current-state snapshot only (`wf-20260726-docs-132`). They are
> preserved here verbatim as the narrative record. **Historical — do not treat any
> statement below as current fact.** Newest first.

**Last updated:** 2026-07-23 — `wf-20260723-fix-127` MERGED (PR [#51](https://github.com/aiqadam/ai-qadam-platform/pull/51), squash `e3edfa7`). ISS-USR-REG-002 resolved: `POST /v1/auth/register` returned a bare, undiagnosable `500 Internal Server Error` on QA for a well-formed request body (reported by the user in chat after the original "400 all-fields-Required" screenshot turned out to be a stale symptom, superseded by the same-day CSRF fix in `wf-20260723-fix-126`). Root cause, confirmed via live Playwright repro against `qa.aiqadam.org` plus a successful local repro with valid credentials: `RegistrationService.register()` had 3 external Authentik-API call sites with zero try/catch (`getUserByEmail`, `resolveGroupNames`+`setUserGroups`, `createRecoveryLink`) and a 4th (`createUser`) that only handled 4xx errors — `AuthentikError` extends plain `Error`, not `HttpException`, so any uncaught throw rendered as an opaque 500. Fix: wrapped all 4 in try/catch, converting failures to the same generic `BadRequestException('registration_failed')` already used elsewhere in the method, with structured logging; Step 5 applies the existing orphan-mitigation pattern; Step 8 (recovery-link mint) is the one exception — logs loudly but does not fail an already-succeeded registration. SecurityReviewer confirmed this does not reopen the email-enumeration oracle already fixed once on this exact method (ISS-USR-REG-001's MAJOR-1) — all new failure paths return the byte-identical `400`, conditioned on upstream availability, not per-email existence. 6 new regression tests (14/14 passing). **AC-4 (live QA verification) deferred with a named, queued follow-up:** [wf-20260723-fix-128-deploy-qa-permission-fix](../tasks/queued/wf-20260723-fix-128-deploy-qa-permission-fix/handoff.yaml) — `deploy-qa` CI has failed on every push to `main` since PR #45 (`unable to unlink old 'package.json': Permission denied` on the QA deploy host), so QA is currently pinned to PR #44's code and cannot receive this fix until that separate blocker is resolved. Counter bumped 127 → 128 (then 128 → 129 for the queued follow-up's placeholder). GitHub issue #50 closed. Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-23 — `wf-20260723-fix-126` MERGED (PR [#42](https://github.com/aiqadam/ai-qadam-platform/pull/42), squash `d0536ac`). GitHub issue #41 resolved: registration form `POST /api/v1/auth/register` returned 403 "Cross-site POST form submissions are forbidden" on `qa.aiqadam.org`. Root cause: Astro's `checkOrigin` middleware compared `url.origin: http://qa.aiqadam.org` (Astro built URL with HTTP since Nginx→Astro connection is unencrypted) against `Origin: https://qa.aiqadam.org` from the browser — scheme mismatch. Fix: added `security.allowedDomains` to `apps/web-next/astro.config.mjs` with the three deployment hostnames. This enables `#applyForwardedHeaders()` which reads the `X-Forwarded-Proto: https` header Nginx already forwards and patches `url.origin` to `https://` before `checkOrigin` runs. CSRF protection remains ON (`checkOrigin: true` unchanged). Counter bumped 126 → 127.

---

**Last updated:** 2026-07-20 — `wf-20260720-feat-125` MERGED (PR [#39](https://github.com/aiqadam/ai-qadam-platform/pull/39), squash `77e21ed`). GitHub issue #38 resolved: anonymous users now see a **Register** button in the top-nav auth cluster (`AppNav.astro`) alongside the existing Sign-in CTA. Clicking Register navigates to `/auth/sign-up` (the custom-branded self-registration page shipped in ISS-USR-REG-001). Change is purely additive — outline-style secondary button, Sign-in remains primary CTA. `docs/04-development/architecture/blocks.md` `<AppNav>` entry updated per ADR-0038 §Locks #4 (pre-commit arch:check enforced). Counter bumped 125 → 126.

---

**Last updated:** 2026-07-18 — `wf-20260718-feat-124` MERGED (PR [#35](https://github.com/aiqadam/ai-qadam-platform/pull/35)). New `docs/04-development/github-access.md` — standalone, machine-agnostic checklist of what's required to operate this repo's GitHub workflow automation, requested directly by the user as an onboarding/standardization doc distinct from `.claude/CLAUDE.md`'s per-machine troubleshooting narrative. Covers: (1) `gh` CLI scopes (`repo`, `workflow`, `read:org`) and why each is needed; (2) `gh`'s default-repo drift check (`gh repo view --json owner,name`); (3) git push transport (SSH vs HTTPS+credential-helper, either works, don't assume which); (4) the `main` branch repository ruleset — confirmed read-only awareness is sufficient (no elevated scope needed unless actually modifying the ruleset, which no documented workflow step does) and the correct API to check it (`rulesets`, not `branches/main/protection`); (5) confirmed GitHub Projects (boards/ProjectV2) is NOT used anywhere in this repo's workflow — issue tracking is GitHub Issues + the local `.copilot/issues/registry.md` mirror only; flagged that Projects integration would need a separate `project` scope if ever required; (6) CI secrets (`COOLIFY_TOKEN`, `PROD_SSH_DEPLOY_KEY`, etc.) are repo-level GitHub Actions secrets, provisioned separately from any individual `gh auth`, not needed for local/agentic workflow steps; (7) the `GITHUB_TOKEN` fallback path in `workflow-finish.sh`. Cross-linked from `docs/04-development/README.md`, `workflow.md` (branch-protection line), and `.claude/CLAUDE.md` (git-credentials section header). All factual claims verified directly against live repo/gh state (`gh auth status`, grep of every `gh` subcommand used in `.copilot/`+`scripts/`, `.github/workflows/*.yml` secrets list) rather than inferred. Explicitly scoped to GitHub access only — infrastructure/server credentials (Coolify, SSH to prod/QA, Authentik admin tokens) are out of scope for this doc, flagged as a separate, higher-blast-radius credential set the user indicated they'll provide separately for harder tasks. Counter bumped 124 → 125. Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-18 — `wf-20260718-feat-123` MERGED (PR [#33](https://github.com/aiqadam/ai-qadam-platform/pull/33)). FR-WORKFLOW-006 shipped: 6 doc-only fixes for session friction discovered firsthand across the two prior workflows this session (`wf-20260718-feat-121` / FR-WORKFLOW-005 and `wf-20260718-fix-122` / ISS-USR-REG-001). (1) `issue-resolution.md` Step 1 now posts a GitHub-issue back-reference comment immediately on intake (not deferred to close-out), and documents resolving ambiguous/underspecified issues with the user before implementation. (2) Step 12.5's closing comment template now requires naming the local `ISS-<n>` file path. (3) **`main` on `aiqadam/ai-qadam-platform` is covered by an active repository ruleset (id `18687633`, requiring `pull_request`) — not classic branch protection, so `gh api repos/<org>/<repo>/branches/main/protection` 404s even when it's actively enforcing; use `gh api repos/<org>/<repo>/rulesets` instead.** Both `issue-resolution.md` Step 12.5 and `requirement-development.md` Step 11.5 (previously describing a direct `git push origin main` for the archive/backfill close-out) now route through a small doc-only PR by default, with a merge-poll requirement before declaring the workflow complete. Same fix applied to `pr-steward.md`'s auto-register flow (had the identical unfixed direct-push bug, found during self-review, including a literal placeholder PR body). (4) `.claude/CLAUDE.md` documents `gh`'s cached default-repo silently drifting from the actual git remote after the `ISS-MIGRATE-001` origin migration (symptom: `gh repo view --json owner,name` shows the old `tvolodi` owner; fix: `gh repo set-default aiqadam/ai-qadam-platform`) — this was the actual root cause of the `gh pr create` failure disclosed in `wf-20260718-feat-121`'s entry below. (5) `.claude/CLAUDE.md` documents `MSYS_NO_PATHCONV=1` as required for `git show <ref>:<path>` and `scripts/check-workflow-state.sh` on this Windows/Git-Bash workstation. (6) `protocol.md` gained an explicit Orchestrator self-consistency check: gate `status` must be verified against the agent's own findings before advancing, citing the concrete incident that motivated it (SecurityReviewer self-reporting `passed` with 3 MAJOR findings listed, during `wf-20260718-fix-122`). **Self-review caught 2 real factual errors before merge**: the branch-protection claim was initially checked with the wrong GitHub API (classic branch-protection endpoint, which doesn't see rulesets) and looked false; `git push --dry-run` was initially documented as a verification method but doesn't reliably reproduce a ruleset rejection — both corrected. Counter bumped 123 → 124. Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-18 — `wf-20260718-fix-122` MERGED (PR [#31](https://github.com/aiqadam/ai-qadam-platform/pull/31), squash `dd5ceef`). ISS-USR-REG-001 resolved: AI-Qadam-branded self-registration, reported via [GitHub issue #28](https://github.com/aiqadam/ai-qadam-platform/issues/28). Scope pre-clarified with the reporter before implementation (posted as a GitHub issue comment): "chapter" = the existing country tenant selector (no new entity), "advantages like a subscribed user" = full member role (`role: member`, no new subscription tier), UI = a custom branded sign-up page (not a bare Authentik redirect). Shipped: new `POST /v1/auth/register` (public, rate-limited 5/15min) on the existing `AuthController`, backed by a new `RegistrationService` (Authentik user creation + password + `aiqadam-member` group assignment + Directus `country` write + one-time-login email dispatch); new `apps/web-next/src/pages/auth/sign-up.astro` + `SignUpForm.tsx` (native `<form method="POST">`, mirrors `LeadCaptureForm.tsx`). **Security find-and-fix cycle**: SecurityReviewer's first pass found 3 MAJOR findings (initially mis-self-reported as `status: passed`, a misapplication of its own gate semantics — corrected by the Orchestrator to `failed-retry` and routed back to CodeDeveloper): (1) the `Location` header of the registration redirect was a deterministic email-enumeration oracle (real Authentik recovery URL for new registrations vs. literal `/v1/auth/login` for duplicates/honeypot) — fixed by delivering the recovery link out-of-band via email (`InteractionsService`, mirroring `leads.service.ts`'s `dispatchVerifyEmail` precedent) so all three outcomes now return the byte-identical redirect; (2) the anti-spam honeypot field was named literally `honeypot` (trivially bot-detectable) — renamed to `company`, matching `LeadCaptureForm.tsx`; (3) length-only `min(12)` password policy on a public endpoint — added `apps/api/src/lib/password-schema.ts` (all-one-character + ~38-entry common-password blocklist rejection). SecurityReviewer's re-review independently confirmed all 3 genuinely closed with no new issues. New regression tests: `apps/api/test/registration-service.spec.ts` (8 tests — happy path explicitly framed as the mandatory issue-resolution "would have failed before the fix" case and the direct MAJOR-1 guard; duplicate-email non-leak; orphaned-account rollback incl. secondary-failure case; Directus/email best-effort-failure non-fatality; username derivation) + `apps/api/test/password-schema.spec.ts` (9 tests, added directly by the Orchestrator after TestRunner flagged the file as an uncovered security-relevant gap). TestRunner corrected an inaccurate "0 failures" claim from TestDesigner's single lucky run: the full `apps/api` suite has 1 pre-existing, unrelated flake at `test/users.spec.ts:65` (timestamp-precision race, file untouched by this PR, already tracked since 2026-07-04 as `wf-20260704-fix-096-pre-existing-api-test-flakes`, apparently never executed) — disclosed honestly, correctly non-blocking. All 7 effective ACs verified (unit-test + code-read tier; live E2E correctly deferred as not yet applicable — feature isn't deployed anywhere). Counter bumped 122 → 123. GitHub issue #28 closed with a summary comment. Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-18 — `wf-20260718-feat-121` MERGED (PR [#29](https://github.com/aiqadam/ai-qadam-platform/pull/29), squash `165cd1d`). FR-WORKFLOW-005 shipped: read-only QA target mode for agent-driven UAT. Adds an explicit `target: local | qa` selector to the `uat-verification` workflow and UATRunner agent (`uat_target` field in `handoff.schema.yaml`, default `local`). `target: qa` resolves the session landing URL to `https://qa.aiqadam.org`, runs a new HTTPS reachability pre-flight (`scripts/uat-qa-preflight-check.sh`, mirrors `uat-preflight-check.sh`'s idiom) against `qa.aiqadam.org` + `auth.qa.aiqadam.org` instead of the Docker/localhost checks, and structurally never invokes seed/reset against QA (no `pnpm uat:seed` code path exists in the new script — verified by a bats regression guard, not just documented). Also closed a pre-existing gap: `uat-runner.md` referenced a `landingUrl` variable in session setup that was never actually assigned — now explicit for both targets. `uat-verification.md`'s Scope Constraints revised from a two-state "must be localhost" rule to a three-state allowlist (`local` default / `qa` explicit opt-in / everything else, including `prod`, hard-blocked at Step 0 with `failed-escalate`). Seed/reset against QA remains explicitly out of scope (QA is read-only) — candidate for a future FR. New: `scripts/uat-qa-preflight-check.sh` + `scripts/tests/uat-qa-preflight-check.bats` (14/14 passing). QualityGate closed the one live-verification gap directly by running the new script against the real deployed QA hosts (not deferred): `https://qa.aiqadam.org` → HTTP 200, `https://auth.qa.aiqadam.org` → HTTP 302, independently re-confirmed by the Orchestrator. Full bats suite showed 10 pre-existing, unrelated failures confined to `scripts/tests/check-workflow-state.bats`, confirmed via `git stash` isolation against a clean baseline (not caused by this PR). All 7 ACs verified, zero deferred. Note: `gh`'s cached default-repo resolution had drifted to `tvolodi/aiqadam` while `origin` actually points at `aiqadam/ai-qadam-platform` — `workflow-finish.sh`'s `gh pr create` failed silently into the compare-URL fallback as a result; fixed via `gh repo set-default aiqadam/ai-qadam-platform`, PR created manually, and `handoff.yaml`'s `github_pr_url` corrected in a follow-up commit before merge. Counter bumped 121 → 122. Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-06 — `wf-20260706-uat-114-bp-uat-013` MERGED (PR [#128](https://github.com/tvolodi/aiqadam/pull/128) squash `d5c9951`). BP-UAT-013 verification run: 10/12 Playwright tests pass, 5/7 ACs verified. ACs 3, 4, 5 (all sub-paths), 6, 7 fully verified. ACs 1 (email delivery) + 2 blocked by env limitation (RESEND_API_KEY not set in apps/api/.env — pre-documented in spec header). Spec fixes applied: Neg005 domain assertion corrected (@aiqadam.test → @example.com); React form submit timing guards added to Steps 001/004 and Neg001. New issue registered: ISS-UAT-013-17 (minor/uat/seed — `reset_domain_fixture()` does not look up or set `authentik_user_id` in `operator_invites` rows; manual Directus PATCH required after each `--reset`). All three post-session enforcement scripts passed: navigation PASS, visual evidence PASS, teardown PASS. Run-scoped evidence: `apps/e2e/uat-results/BP-UAT-013/wf-20260706-uat-114-bp-uat-013/`. BP-UAT-013 registry: last_run=2026-07-06, status=partial. Counter bumped 114 → 115. Close-out commit `9dd958c` pushed; workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-05 — `wf-20260705-fix-114-bp-uat-013-fixture-lookup-unique` MERGED (PR [#123](https://github.com/tvolodi/aiqadam/pull/123) squash `b20a1ef`). ISS-UAT-013-16 RESOLVED: applied Option A from issue body — per-fixture `lookup_field: token_prefix → token_hash` + `lookup_value: uat-onbo → sha256(token_plain)`. `reset_domain_fixture()` now DELETEs only the row matching each fixture's exact sha256 — each fixture's CREATE survives. Bonus fix in same PR: aligned `payload.email` @aiqadam.test → @example.com (Directus `is-email` validator; `wf-20260704-fix-086` / ISS-UAT-BRIDGE-002 TLD migration hadn't propagated to this manifest). 6 new bats regression rows (42-47) in `scripts/tests/uat-seed.bats` — 47/47 pass. Live verification: AC-1 Directus query → 4 rows (valid/used/expired/no-user); AC-2 preview `uat-onboard-token` → 200 with `uat-operator@example.com`; AC-3 used → 410 `invite_consumed`, expired → 410 `invite_expired`, no-user → 200 with `uat-operator+no-user@example.com`; AC-4 unconditional seed not regressed (mock-mode); AC-5 47/47 bats. Idempotency check: 2nd `--reset BP-UAT-013` shows each fixture's DELETE matches exactly ONE row. ISS-UAT-013-16 follows the AGENTS.md §6.1 honesty disclosure: AC-4 was verified in mock-mode (live re-run would mutate other BP-UAT fixtures). Once `wf-20260705-uat-110-bp-uat-013-verify` re-runs (or its renamed successor), BP-UAT-013 AC-2/5/6/7 should flip from failed to passing. Counter bumped 110 → 111. Auto-merge per AGENTS.md §6.2 + §6.3 user CI opt-out (PR #123 was squashed and merged at 2026-07-05 09:37:41 UTC with `--auto --squash --delete-branch`). Workflow directory archived `active/` → `completed/`.

---

**Last updated:** 2026-07-05 — `wf-20260705-uat-110-bp-uat-013-verify` MERGED (PR [#122](https://github.com/tvolodi/aiqadam/pull/122) squash `723346b`). ISS-UAT-013-16 registered: `--reset BP-UAT-013` manifest uses non-unique `lookup_field: token_prefix` shared by all 4 fixtures, so `reset_domain_fixture()`'s DELETE wipes each preceding CREATE — only the LAST fixture's row survives. Discovered during Step 3 Playwright run (5/12 passed, 7 failed with 410 `invite_invalid` for missing rows). Both pre-flight fixes (ISS-UAT-013-14 PR #119, ISS-UAT-013-15 PR #120) were correctly applied; the manifest non-uniqueness is the NEXT gap in the same `--reset` code path that ISS-UAT-013-14 did not address. BP-UAT-013 AC-by-AC: 2/7 verified (AC-3 idempotency, AC-4 honeypot), 1/7 partial (AC-1 UI side), 4/7 failed (AC-2/5/6/7 — ISS-UAT-013-16). Pre-flight had a hidden observability gap: seed's stdout reported "4 fixture(s) created" but Directus only contained 1 row (now flagged for future pre-flight refinement). Follow-up workflow `wf-20260705-fix-113-bp-uat-013-fixture-lookup-unique` queued at queue_position 4 with `blocks: [wf-20260705-uat-110]`. Once it lands + merges, this UAT verification workflow's Step 3 should be re-run from scratch to verify all 7 ACs end-to-end against the live stack. Three sub-gaps surfaced as Honesty disclosures (not filed as new issues): (1) `apps/e2e/playwright.uat.config.ts` doesn't load `apps/e2e/.env.uat` so `UAT_API_URL` falls back to `:3001`; (2) `BP-UAT-013.md` doc says `@aiqadam.test` but spec uses `@example.com` — drift introduced post-ISS-UAT-BRIDGE-002; (3) Directus `is-email` validator rejects `.example.com` per RFC 2606. Counter bumped 109 → 110. Auto-merge per AGENTS.md §6.2 + §6.3 user CI opt-out (PR #122 was squashed and merged at 2026-07-05 08:49:15 UTC with `--auto --squash --delete-branch`). Close-out commit `c6bd686` pushed; workflow directory archived `.copilot/tasks/active/wf-20260705-uat-110-bp-uat-013-verify/` → `.copilot/tasks/completed/wf-20260705-uat-110-bp-uat-013-verify/`. Registry row updated: BP-UAT-013 Run Status = `partial`; Last Run preserved at `2026-07-02` (honest prior state per AGENTS.md §6.1).

---

**Last updated:** 2026-07-05 — `wf-20260705-fix-105` MERGED (PR #120 squash `f55ce74`). ISS-UAT-013-15 resolved: `scripts/uat-seed.sh` is now MSYS-aware. Top of script resolves `CURL_BIN='curl.exe'` when `command -v curl.exe` succeeds, otherwise falls back to bare `curl`. All 14 runtime `curl` invocations route through `"$CURL_BIN"`. `check_deps()` extended with `command -v "$CURL_BIN"` guard + `'Missing required curl binary'` message. 4 new bats rows (38-41) added to `scripts/tests/uat-seed.bats` — bats 41/41 pass. AGENTS.md §6.1 subsection "Shell-script HTTP client binary selection (added 2026-07-05, ISS-UAT-013-15)" documents the canonical curl.exe-detection idiom for future scripts. Refinement vs. issue body (recorded per §13 in PR Risks): chose `command -v curl.exe` over the issue's `uname -s | grep mingw` heuristic — strictly broader (covers WSL bash too), matches the `uat-preflight-email.sh` precedent. AC-2 verified 4/4. AC-1 (live `bash scripts/uat-seed.sh` from MSYS) + AC-4 (BP-UAT-013 live re-run) deferred with honesty disclosure to queued `wf-20260705-fix-103-uat-013-verify` (queue position 3). AC-3 (Path B AGENTS.md note) moot — Path A landed and supersedes the workaround. Counter bumped 106 → 107. Auto-merge per AGENTS.md §6.2 + §6.3 user CI opt-out (PR was squashed and merged at 2026-07-05 06:26:17 UTC with `--auto --squash --delete-branch`). `wf-20260705-uat-100` parent workflow remains STOPPED at Step 2 with `failed-escalate` — needs-review.md unreviewed. Active workflow directory `.copilot/tasks/active/wf-20260705-fix-105/` archived → `.copilot/tasks/completed/wf-20260705-fix-105/`.

---

**Last updated:** 2026-07-05 — `wf-20260705-uat-100` STOPPED at Step 2 (Pre-Flight) `failed-escalate`. User-requested BP-UAT-013 re-verification (`Run UAT testing for BP-UAT-013`). Step 1 (BusinessAnalyst script validation) PASSED; Step 2 (pre-flight) failed on two distinct non-product issues: (a) `scripts/uat-seed.sh --reset BP-UAT-013` POSTs manifests without `token_hash`/`token_prefix` (Directus 400 — registered as [ISS-UAT-013-14](../issues/ISS-UAT-013-14.md), queue position 1 → [wf-20260705-fix-101-bp-uat-013-seed-reset](../tasks/queued/wf-20260705-fix-101-bp-uat-013-seed-reset/handoff.yaml)); (b) bash GNU curl from inside this machine's Copilot-Chat terminal sandbox cannot reach Windows-host `localhost:<api-port>` even though `curl.exe` from the same PowerShell reaches `:3001` HTTP 200 (registered as [ISS-UAT-013-15](../issues/ISS-UAT-013-15.md), queue position 2 → [wf-20260705-fix-102-uat-seed-curl-exe-aware](../tasks/queued/wf-20260705-fix-102-uat-seed-curl-exe-aware/handoff.yaml)). Both fixes must land before [wf-20260705-fix-103-uat-013-verify](../tasks/queued/wf-20260705-fix-103-uat-013-verify/handoff.yaml) (queue position 3 — the actual Playwright UAT run) can complete. Cleanup: api I started on `:3001` (PID 5488) was stopped; `operator_invites` table is empty (4 rows deleted by failed `--reset`); `apps/e2e/uat-results/BP-UAT-013/` is empty (no Playwright run occurred); `registry.md` BP-UAT-013 row's `last_run` is HONESTLY unchanged at 2026-07-02 — no run completed, so no `last_run` bump. Active task artifacts at [`.copilot/tasks/active/wf-20260705-uat-100/`](../tasks/active/wf-20260705-uat-100/) (status `needs-review`); NEEDS_REVIEW at [`NEEDS_REVIEW.md`](../tasks/active/wf-20260705-uat-100/NEEDS_REVIEW.md).

---

---

**Superseded entry, retained for delta-only history:**

**Last updated:** 2026-07-04 — `wf-20260704-fix-095` merged. [PR #110](https://github.com/tvolodi/aiqadam/pull/110) squash `69b2bc6` (ISS-TEST-WEB-001 — root cause: `vitest ^2.1.8` (pinned in apps/web, apps/web-next, apps/api) bundles vite 5.x/6.x whose SSR transform is missing `__vite_ssr_exportName__` (added in vite v8). The workspace's hoisted `vite@8.1.0` defines that helper, so any cross-module `import` in a test crashed the suite load with `ReferenceError: __vite_ssr_exportName__ is not defined`, blocking ISS-UAT-013-13 AC-3 regression test. Fix: bump `vitest ^2.1.8` → `^4.1.9` in all three apps + `@vitest/coverage-v8 ^2.1.8` → `^4.1.9` in apps/api (vitest 4.1.9 declares peer `vite: ^6.0.0 || ^7.0.0 || ^8.0.0`, satisfied by vite 8.1.0); removed obsolete `transformMode: 'web'` from `apps/api/vitest.unit.config.ts` (option removed in vitest 3.0); wired `@vitejs/plugin-react@^5.2.0` as first plugin in `apps/web-next/vitest.config.ts` (mirroring PR #109 / ISS-CI-OVERRIDE-ebd184b storybook pattern) so `.tsx` test files parse under vite 8.1.0/rolldown. Regression test `apps/web/src/components/OnboardingForm.test.ts` 5/5 pass (was blocked pre-fix); apps/web 54/54; apps/web-next 923/923 across 33 files; apps/api unit-config 15/15; apps/api full Testcontainers 1251/1257 (6 pre-existing test-design bugs owned by `wf-20260704-fix-096-pre-existing-api-test-flakes` — none caused by this PR, they were masked by the `__vite_ssr_exportName__` block). biome 0 errors; astro check 0 errors. Counter bumped 96 → 97. Auto-merge per AGENTS.md §6.2 + §6.3 user CI opt-out. Predecessor `wf-20260704-fix-093` (PR #109 squash `255d2bb`).

---

---
