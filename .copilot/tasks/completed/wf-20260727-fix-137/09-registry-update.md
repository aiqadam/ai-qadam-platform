# Step 9: Registry Update (atomic) — wf-20260727-fix-137

## Edit 1 — `.copilot/issues/ISS-AUTH-OIDC-EMAIL-001.md`

- `Status`: `in-progress` → `resolved`
- `Resolved`: `—` → `2026-07-27`
- Resolution section already had `Root cause`/`Fix`/`Regression test`
  filled at creation (Step 1); `PR`/`Merged` remain `<pending>` until
  Step 12/12.5.

## Edit 2 — `.copilot/issues/registry.md`

- Row `Status`: `in-progress` → `resolved`
- Row summary expanded to state the fix and verification method
- `Workflow`/`Date` unchanged (already correct: `wf-20260727-fix-137` /
  `2026-07-27`)

Both edits staged and committed together in the same commit as the code
fix (see Step 12).

## gate_result

```yaml
status: passed
step: 9
timestamp: "2026-07-27T00:00:00Z"
summary: "Both ISS-AUTH-OIDC-EMAIL-001.md and registry.md flipped to resolved atomically."
```
