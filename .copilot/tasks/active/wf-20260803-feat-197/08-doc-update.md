# Documentation Update — FEAT-NTF-004 (Telegram notification adapter gap-fill)

Workflow: `wf-20260803-feat-197`

Scope: AC-10 only, per the atomic-pair protocol — correct `FR-NTF-004.md`'s
stale architecture text and flip its status, plus the matching
`requirements-registry.md` row. No other documentation file touched.

---

## Documents Updated

| Document | Section | Change Description |
|---|---|---|
| `docs/03-requirements/FR-NTF-004.md` | Frontmatter | `status: Planned` → `status: Implemented`. Added `business_process: —` (was absent; confirmed per the validated requirement's explicit decision NOT to link `BP-UAT-010` — its own AC-3/Step-003/Notes scope is "an email arrived in the mail-catcher," with no fixture/step/assertion touching Telegram content, buttons, or sanitization). |
| `docs/03-requirements/FR-NTF-004.md` | Description | Added a dated correction blockquote (`> **2026-08-03 correction (wf-20260803-feat-197):**`), matching ADR-0034's own "2026-07-31 update" callout style/tone/placement (original prose paragraph preserved above it, unmodified, for historical contrast — same pattern ADR-0034 uses for its Q1 Coolify correction). Explains the 3 concrete mismatches between the original text and the shipped ADR-0034 design (direct-Telegram-API-call claim, Authentik `attributes.telegram_id`, BullMQ rate limiter) and states plainly that no FR-NTF-004 code existed before this correction, so nothing was ever built against the stale description. |
| `docs/03-requirements/FR-NTF-004.md` | Functional scope item 1 (Channel adapter) | Rewrote the lookup path from `userId → Authentik attributes.telegram_id → Telegram Bot API sendMessage` to `userId → directus_users.telegram_user_id → tg.dispatch.v1 envelope → Postgres outbox → Redis Streams → Python notifier → Telegram Bot API sendMessage`, naming the real class (`TelegramAdapter`) and file path, and stating the adapter's own observable boundary (`state: 'sent'` = durably outboxed, not phone-delivered). |
| `docs/03-requirements/FR-NTF-004.md` | Functional scope item 2 (Eligibility check) | Rewrote to `directus_users.telegram_user_id` set AND `telegram_opted_out_at` null (opt-out semantics, not the fictional `notification_telegram_enabled` opt-in flag), attributed to `InteractionsService.resolveRecipients()`; corrected the skip behavior to match what's actually implemented (clean skip, no error, no delivery row) rather than the old `{status:'skipped', reason:...}` + `notifications_sent` claim. |
| `docs/03-requirements/FR-NTF-004.md` | Functional scope item 4 (Inline buttons) | Added the "no button" case for registration-waitlisted, matching what shipped (mirrors the buttonless waitlisted email template) — the original list only covered confirmed/reminder/promoted. |
| `docs/03-requirements/FR-NTF-004.md` | Functional scope item 5 (Rate limiting) | Rewrote to attribute rate-limiting entirely to the separate Python notifier process per ADR-0034 §Q2/Q6, removed the false "existing BullMQ outbox/dispatcher rate limiter" claim, quoted ADR-0034's own Risks-table line distinguishing BullMQ (internal NestJS jobs) from Streams (cross-language/cross-service). |
| `docs/03-requirements/FR-NTF-004.md` | Functional scope items 6–7 (Failure handling, Audit) | Rewrote the audit trail from a fictional `notifications_sent` table to the real `interaction_deliveries` collection + `tg_send_log` (notifier's idempotent send-log, keyed on `delivery_key`), explicitly stating `notifications_sent` does not exist for Telegram. |
| `docs/03-requirements/FR-NTF-004.md` | Acceptance criteria | Corrected AC-6 (previously "the bot service is not involved in outbound DM sending... all outbound comes from the NestJS API directly" — false; rewrote to state the separate Python notifier process, distinct from the inbound long-poll bot process per ADR-0034 §Q4, is what calls `sendMessage`, and flagged the original wording as corrected). Marked the ACs actually live-verified per `07-test-results.md` as `[x]` (AC-5/6/7 registration-flow delivery, AC-8 ineligible-skip, the inline-button AC, and the corrected AC-6) — each with an explicit note that verification stops at outbox/`interaction_deliveries` state, never actual phone-side Telegram delivery. Left the blocked-user-failure AC and the 30/sec rate-limit AC as `[ ]` (unchecked) with notes explaining both require the notifier's own not-yet-fully-built `sendMessage`/rate-limit implementation, which this workflow's NestJS-only diff cannot verify — not overclaimed. |
| `docs/03-requirements/FR-NTF-004.md` | Notes | Rewrote the FR-AUTH-005 dependency note to state it's not a real dependency (wrong field — FR-AUTH-005 would populate an Authentik attribute this FR never reads; the real eligibility fields already work today, independent of FR-AUTH-005 shipping). Added: (1) an ADR-0034-sourced architecture summary; (2) a note that the notifier's own `sendMessage`/rate-limiting code is a separately-scoped, not-yet-fully-built piece, plus the live-confirmed fact that the outbox-write path itself has zero Redis dependency; (3) the sanitizer's known residual limitation (well-formed allowlisted-tag sequences in operator-authored event titles survive `sanitizeTelegramHtml()` unchanged) as an accepted Phase-1 risk, with an explicit, non-overstated distinction that MAJOR-2's title `"`/`\` regex guard is a *related but different* protection (blocks quote/backslash only, not HTML tag syntax) and does not close this gap; (4) a second, distinct known-limitation bullet naming that MAJOR-2's fix covers only the NestJS `PATCH` operator-edit path, not Directus-direct event-title creation (per the security review's re-review, which explicitly asked for this to be captured at this step); (5) a brief "what shipped" implementation-note bullet naming the actual files (`telegram-adapter.ts`, `telegram-html-sanitizer.ts`, the 3 Directus flow ops in `flows-bootstrap.sh`) and pointing at `03-code-summary.md` for the full trace — sized to match the brevity of other Implemented FRs' final Notes bullets (e.g. `FR-AUTH-006.md`'s pointer to its own validation doc). |
| `docs/03-requirements/requirements-registry.md` | Row 60 (FR implementation order table) | `Status` column: `Planned` → `Shipped`. |

## Documents Not Updated

- `docs/04-development/architecture/architecture.md` — out of scope for this workflow (AC-10 only covers the FR + registry correction; no module-boundary change here, and ADR-0034 already documents the architecture authoritatively).
- `docs/04-development/security/security.md` — explicitly out of scope per the task brief. The sanitizer's residual-risk note and the title-guard's scope limitation both live in `FR-NTF-004.md`'s own Notes section instead, per instruction.
- New ADR — not needed; ADR-0034 already covers the architecture this correction aligns FR-NTF-004.md to. Nothing architectural is being decided in this workflow.
- `docs/03-requirements/FR-NTF-001.md` — noticed during research that this sibling FR's own text (`notifications_sent` table, a `dispatcher.service.ts` signature that doesn't match the real `InteractionsService`) is *also* stale relative to the same ADR-0034/outbox reality FR-NTF-004.md was just corrected against. Not touched here — out of this workflow's scope (AC-10 names only FR-NTF-004.md), but flagging it as a candidate follow-up doc-correction workflow, same class as this one.
- `docs/03-requirements/FR-AUTH-005.md` — read only for cross-reference (confirming it's still genuinely `Planned` and describes a different field). Not modified; this workflow doesn't touch its scope.
- `packages/shared-types/README.md` — no new shared-types schema introduced by this workflow's code changes (per `03-code-summary.md`, the `inline_buttons` Zod schema lives entirely inside `TelegramAdapter`'s own file, not `packages/shared-types`).

## GitHub Sync

```
scripts/sync-github-project.sh --ref FR-NTF-004 --status implemented --existing-url "https://github.com/aiqadam/ai-qadam-platform/issues/142"
```

Result: succeeded. Script exited 0 (runs under `set -euo pipefail`, so any
failure inside would have aborted before the final line) and printed:

```
GITHUB_ISSUE_URL=https://github.com/aiqadam/ai-qadam-platform/issues/142
```

Confirms the Project board's Status field for issue #142 was set to
"implemented." Best-effort/idempotent per protocol.md — this does not
affect the gate status below regardless of outcome, but it succeeded.

## Gate Result

```yaml
gate_result:
  status: passed
  summary: "AC-10 fully satisfied. FR-NTF-004.md corrected in place: dated 2026-08-03 correction blockquote added to Description (matching ADR-0034's own '2026-07-31 update' callout style — original stale prose preserved above it for historical contrast, not deleted); Functional scope items 1, 2, 5, 6, 7 rewritten to describe the real shipped outbox -> Redis Streams (tg.dispatch.v1) -> Python notifier design per ADR-0034, removing every trace of the fictional Authentik attributes.telegram_id lookup, BullMQ rate limiter, and notifications_sent table; AC-6 corrected (the bot's inbound process isn't involved, but a separate notifier process is, per ADR-0034 Q4 -- the original 'not involved at all' wording was false and is now fixed); Notes section's FR-AUTH-005 dependency claim corrected (wrong field, not a real blocking dependency); status frontmatter flipped Planned -> Implemented; business_process frontmatter explicitly set to '—' (confirmed, not linking BP-UAT-010, per the validated requirement's own reasoning). Sanitizer residual-risk note added per the security review's recommendation, with an accurate, non-overstated distinction that MAJOR-2's title regex guard (quote/backslash only) is related but does not close the HTML-tag-in-title sanitizer gap; a second known-limitation bullet also captures that the title guard covers only the NestJS PATCH path, not Directus-direct title creation, per the security review's re-review request. A brief 'what shipped' implementation note points at the 3 actual changed/new files and the code summary, sized to match other Implemented FRs' brevity convention (checked FR-BOT-001.md, FR-AUTH-006.md, FR-MIG-024.md for precedent). Acceptance criteria checkboxes updated to reflect exactly what 07-test-results.md live-verified (AC-5 through AC-8 equivalents checked, against outbox/interaction_deliveries state only) versus what remains unverifiable pending the notifier's own not-yet-built sendMessage/rate-limit code (left unchecked, each with an honest note, not overclaimed). requirements-registry.md row 60's Status column flipped Planned -> Shipped in the same pass, satisfying the atomicity requirement -- both files modified together in this step. GitHub sync ran successfully (non-blocking either way): issue #142's Project board Status set to 'implemented', confirmed via the script's GITHUB_ISSUE_URL success output under set -euo pipefail. No other documentation file touched -- architecture.md, security.md, and no new ADR, per the task's explicit scope boundary. One incidental finding (not actioned, out of scope): FR-NTF-001.md's own text is also stale relative to the same ADR-0034 reality (notifications_sent, a dispatcher.service.ts signature that doesn't match InteractionsService) -- named as a candidate follow-up, not fixed here."
  findings:
    - "FR-NTF-004.md: all of AC-10's named targets corrected -- Functional scope items 1/2/5/6/7, the AC-6 acceptance criterion, and the Notes' FR-AUTH-005 dependency claim -- verified by direct re-read after writing, no stale Authentik/BullMQ/notifications_sent language remains anywhere in the file outside the historical Description paragraph the correction note explicitly contrasts against (intentionally preserved, matching the ADR-0034/FR-CRM-002 precedent of not deleting the original text)."
    - "status: Implemented (not Superseded) confirmed correct per the validated requirement's explicit disposition guidance -- this is a genuine ship, contrasted directly against FR-CRM-002's supersession precedent in the correction note's own text."
    - "business_process left at '—' (added, was absent in the original file) -- matches the validated requirement's explicit decision not to link BP-UAT-010, and matches the frontmatter convention used by other Implemented FRs with no BP link (FR-BOT-001.md, FR-AUTH-006.md both carry the same 'business_process: —' field)."
    - "Sanitizer residual-risk note and the title-guard scope-limitation note both added to FR-NTF-004.md's own Notes section, per the explicit instruction not to touch security.md for this workflow. The distinction between the two guards (regex blocks quote/backslash only; sanitizer gap is about HTML tag syntax in titles) is stated accurately, not conflated -- checked against both the security review's original MAJOR-2 finding and its retry-1 re-review before writing."
    - "requirements-registry.md row 60 Status column flipped Planned -> Shipped, committed as part of the same DocWriter step as FR-NTF-004.md's status flip, satisfying the atomic-pair requirement (both files modified by this agent in this step; actual git commit happens later at Step 11 per the Orchestrator, per protocol.md)."
    - "GitHub sync succeeded (non-blocking, ran anyway): scripts/sync-github-project.sh --ref FR-NTF-004 --status implemented --existing-url <issue-142-url> exited 0 and printed GITHUB_ISSUE_URL=https://github.com/aiqadam/ai-qadam-platform/issues/142."
    - "Fixed a self-introduced Markdown formatting slip during drafting (a stray unescaped '* **not**' line inside the new correction blockquote that would have broken out of the blockquote and rendered as a bullet) before finalizing the file -- caught by re-reading the written file, not left in."
    - "Incidental, unactioned finding: FR-NTF-001.md (the sibling dispatcher FR this one extends) still describes a notifications_sent table and a dispatcher.service.ts signature that don't match the real InteractionsService/outbox architecture either -- out of this workflow's AC-10 scope (names only FR-NTF-004.md), not touched, flagged in Documents Not Updated as a candidate follow-up doc-correction workflow."
```
