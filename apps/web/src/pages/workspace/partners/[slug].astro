---
import PartnerView from '../../../components/workspace/PartnerView';
import Layout from '../../../layouts/Layout.astro';

// Server-rendered: slug param resolved at request time, not at build
// (so we skip getStaticPaths). Matches /workspace/events/[id].astro.
export const prerender = false;

const { slug } = Astro.params;
---

<Layout
  title="Partner · AI Qadam workspace"
  description="Partner entitlements, audiences, and kit downloads."
>
  <main style="max-width: 1080px; margin: 0 auto; padding: 40px 32px;">
    <p style="margin: 0 0 8px; font-size: 13px;">
      <a href="/workspace/partners">← Partners</a>
    </p>
    <PartnerView slug={slug ?? ''} client:load />
  </main>
</Layout>
